import { ApplicationMetadata } from '@loopback/core';
import { OASEnhancer, OpenAPIObject } from '@loopback/rest';
/**
 * Configuration for IBM API Connect extensions to the OpenAPI spec
 */
export declare type ApiConnectSpecOptions = {
    targetUrl: string;
};
/**
 * An OpenAPI spec enhancer to add `x-ibm-configuration` extension required
 * by API Connect
 */
export declare class ApiConnectSpecEnhancer implements OASEnhancer {
    private options;
    private appMetadata;
    name: string;
    constructor(options: ApiConnectSpecOptions, appMetadata?: ApplicationMetadata);
    modifySpec(spec: OpenAPIObject): OpenAPIObject;
}
