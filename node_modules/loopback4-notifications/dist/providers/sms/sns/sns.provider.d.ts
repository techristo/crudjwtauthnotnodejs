import { Provider } from '@loopback/core';
import { SNS } from 'aws-sdk';
import { SNSMessage, SNSNotification } from './types';
export declare class SnsProvider implements Provider<SNSNotification> {
    private readonly snsConfig?;
    constructor(snsConfig?: SNS.ClientConfiguration | undefined);
    snsService: SNS;
    value(): {
        publish: (message: SNSMessage) => Promise<void>;
    };
}
