import { Provider } from '@loopback/core';
import Pubnub from 'pubnub';
import { Config } from '../../../types';
import { PubNubMessage, PubNubNotification } from './types';
export declare class PubNubProvider implements Provider<PubNubNotification> {
    private readonly pnConfig?;
    constructor(pnConfig?: Pubnub.PubnubConfig | undefined);
    pubnubService: Pubnub;
    value(): {
        publish: (message: PubNubMessage) => Promise<void>;
        grantAccess: (config: Config) => Promise<{
            ttl: any;
        }>;
        revokeAccess: (config: Config) => Promise<{
            success: boolean;
        }>;
    };
}
