/// <reference types="socket.io-client" />
import { Provider } from '@loopback/core';
import { SocketConfig, SocketMessage, SocketNotification } from './types';
export declare class SocketIOProvider implements Provider<SocketNotification> {
    private readonly socketConfig?;
    constructor(socketConfig?: SocketConfig | undefined);
    socketService: SocketIOClient.Socket;
    value(): {
        publish: (message: SocketMessage) => Promise<void>;
    };
}
