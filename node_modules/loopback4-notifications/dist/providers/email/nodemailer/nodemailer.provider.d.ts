import { Provider } from '@loopback/core';
import { INotificationConfig } from '../../../types';
import { NodemailerMessage, NodemailerNotification } from './types';
import SMTPTransport = require('nodemailer/lib/smtp-transport');
import Mail = require('nodemailer/lib/mailer');
export declare class NodemailerProvider implements Provider<NodemailerNotification> {
    private readonly config?;
    private readonly nodemailerConfig?;
    constructor(config?: INotificationConfig | undefined, nodemailerConfig?: SMTPTransport.Options | undefined);
    transporter: Mail;
    value(): {
        publish: (message: NodemailerMessage) => Promise<void>;
    };
}
