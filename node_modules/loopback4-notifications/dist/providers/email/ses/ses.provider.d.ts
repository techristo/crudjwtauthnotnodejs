import { Provider } from '@loopback/core';
import { SES } from 'aws-sdk';
import { INotificationConfig } from '../../../types';
import { SESMessage, SESNotification } from './types';
export declare class SesProvider implements Provider<SESNotification> {
    private readonly config?;
    private readonly sesConfig?;
    constructor(config?: INotificationConfig | undefined, sesConfig?: SES.ClientConfiguration | undefined);
    sesService: SES;
    value(): {
        publish: (message: SESMessage) => Promise<void>;
    };
}
