import { Provider } from '@loopback/core';
import { INotification, Message } from '../types';
export declare class NotificationProvider implements Provider<INotification> {
    private readonly smsProvider?;
    private readonly emailProvider?;
    private readonly pushProvider?;
    constructor(smsProvider?: INotification | undefined, emailProvider?: INotification | undefined, pushProvider?: INotification | undefined);
    publish(data: Message): Promise<void>;
    value(): {
        publish: (message: Message) => Promise<void>;
    };
}
